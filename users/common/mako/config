max-visible=1
anchor=bottom-right
font=Inconsolata

actions=1
icons=1
markup=1

padding=2

background-color={{ .theme.dark }}
progress-color={{ .theme.dark }}
text-color={{ .theme.hint }}

border-color={{ .theme.hint }}
border-size=2
border-radius=0

default-timeout=4000
